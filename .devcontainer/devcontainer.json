{
  "name": "${localWorkspaceFolderBasename} — Nix Dev Environment",
  "build": {
    "context": ".",
    "dockerfile": "Dockerfile"
  },

  "workspaceFolder": "/workspace",
  "workspaceMount": "source=${localWorkspaceFolder},target=/workspace,type=bind,consistency=cached",
  
  "mounts": [
    "source=lunarhue-containers-nix-store,target=/nix,type=volume",
    "source=${localEnv:HOME}/.config/gh,target=/root/.config/gh,type=bind,readonly"
  ],
  
  "containerEnv": { // Environment variables to set inside the container
    "NIX_CONFIG": "experimental-features = nix-command flakes"
  },
  "remoteEnv": { // Environment variables to set in the VS Code Remote environment
    "GH_TOKEN": "${localEnv:GH_TOKEN}",
    "GITHUB_TOKEN": "${localEnv:GITHUB_TOKEN}"
  },
  
  "features": {
    "ghcr.io/devcontainers/features/common-utils:2": {
      "username": "vscode",
      "uid": "1000",
      "gid": "1000",
      "installZsh": false,
      "installOhMyZsh": false,
      "configureZshAsDefaultShell": false
    },
    "ghcr.io/devcontainers/features/docker-outside-of-docker:1": {
      "version": "latest",
      "enableNonRootDocker": "true",
      "moby": "false"
    }
  },

  "postCreateCommand": "bash .devcontainer/create.sh",
  "postStartCommand": "bash .devcontainer/start.sh",
  
  "customizations": {
    "vscode": {
      "extensions": [
        "mkhl.direnv",
        "jnoortheen.nix-ide"
      ]
    }
  },
  
  "remoteUser": "vscode"
}