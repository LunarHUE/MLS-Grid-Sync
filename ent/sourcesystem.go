// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/LunarHUE/MLS-Grid-Sync/ent/sourcesystem"
)

// SourceSystem is the model entity for the SourceSystem schema.
type SourceSystem struct {
	config `json:"-"`
	// ID of the ent.
	// Stable slug like 'mlsgrid'
	ID string `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// ModifiedAt holds the value of the "modified_at" field.
	ModifiedAt time.Time `json:"modified_at,omitempty"`
	// SourceSystemName holds the value of the "source_system_name" field.
	SourceSystemName string `json:"source_system_name,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the SourceSystemQuery when eager-loading is set.
	Edges        SourceSystemEdges `json:"edges"`
	selectValues sql.SelectValues
}

// SourceSystemEdges holds the relations/edges for other nodes in the graph.
type SourceSystemEdges struct {
	// SyncEvents holds the value of the sync_events edge.
	SyncEvents []*SyncEvent `json:"sync_events,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool

	namedSyncEvents map[string][]*SyncEvent
}

// SyncEventsOrErr returns the SyncEvents value or an error if the edge
// was not loaded in eager-loading.
func (e SourceSystemEdges) SyncEventsOrErr() ([]*SyncEvent, error) {
	if e.loadedTypes[0] {
		return e.SyncEvents, nil
	}
	return nil, &NotLoadedError{edge: "sync_events"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*SourceSystem) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case sourcesystem.FieldID, sourcesystem.FieldSourceSystemName:
			values[i] = new(sql.NullString)
		case sourcesystem.FieldCreatedAt, sourcesystem.FieldModifiedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the SourceSystem fields.
func (_m *SourceSystem) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case sourcesystem.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				_m.ID = value.String
			}
		case sourcesystem.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case sourcesystem.FieldModifiedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field modified_at", values[i])
			} else if value.Valid {
				_m.ModifiedAt = value.Time
			}
		case sourcesystem.FieldSourceSystemName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field source_system_name", values[i])
			} else if value.Valid {
				_m.SourceSystemName = value.String
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the SourceSystem.
// This includes values selected through modifiers, order, etc.
func (_m *SourceSystem) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QuerySyncEvents queries the "sync_events" edge of the SourceSystem entity.
func (_m *SourceSystem) QuerySyncEvents() *SyncEventQuery {
	return NewSourceSystemClient(_m.config).QuerySyncEvents(_m)
}

// Update returns a builder for updating this SourceSystem.
// Note that you need to call SourceSystem.Unwrap() before calling this method if this SourceSystem
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *SourceSystem) Update() *SourceSystemUpdateOne {
	return NewSourceSystemClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the SourceSystem entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *SourceSystem) Unwrap() *SourceSystem {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: SourceSystem is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *SourceSystem) String() string {
	var builder strings.Builder
	builder.WriteString("SourceSystem(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("modified_at=")
	builder.WriteString(_m.ModifiedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("source_system_name=")
	builder.WriteString(_m.SourceSystemName)
	builder.WriteByte(')')
	return builder.String()
}

// NamedSyncEvents returns the SyncEvents named value or an error if the edge was not
// loaded in eager-loading with this name.
func (_m *SourceSystem) NamedSyncEvents(name string) ([]*SyncEvent, error) {
	if _m.Edges.namedSyncEvents == nil {
		return nil, &NotLoadedError{edge: name}
	}
	nodes, ok := _m.Edges.namedSyncEvents[name]
	if !ok {
		return nil, &NotLoadedError{edge: name}
	}
	return nodes, nil
}

func (_m *SourceSystem) appendNamedSyncEvents(name string, edges ...*SyncEvent) {
	if _m.Edges.namedSyncEvents == nil {
		_m.Edges.namedSyncEvents = make(map[string][]*SyncEvent)
	}
	if len(edges) == 0 {
		_m.Edges.namedSyncEvents[name] = []*SyncEvent{}
	} else {
		_m.Edges.namedSyncEvents[name] = append(_m.Edges.namedSyncEvents[name], edges...)
	}
}

// SourceSystems is a parsable slice of SourceSystem.
type SourceSystems []*SourceSystem
