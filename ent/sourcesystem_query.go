// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/LunarHUE/MLS-Grid-Sync/ent/predicate"
	"github.com/LunarHUE/MLS-Grid-Sync/ent/sourcesystem"
	"github.com/LunarHUE/MLS-Grid-Sync/ent/syncevent"
)

// SourceSystemQuery is the builder for querying SourceSystem entities.
type SourceSystemQuery struct {
	config
	ctx                 *QueryContext
	order               []sourcesystem.OrderOption
	inters              []Interceptor
	predicates          []predicate.SourceSystem
	withSyncEvents      *SyncEventQuery
	modifiers           []func(*sql.Selector)
	loadTotal           []func(context.Context, []*SourceSystem) error
	withNamedSyncEvents map[string]*SyncEventQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the SourceSystemQuery builder.
func (_q *SourceSystemQuery) Where(ps ...predicate.SourceSystem) *SourceSystemQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *SourceSystemQuery) Limit(limit int) *SourceSystemQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *SourceSystemQuery) Offset(offset int) *SourceSystemQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *SourceSystemQuery) Unique(unique bool) *SourceSystemQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *SourceSystemQuery) Order(o ...sourcesystem.OrderOption) *SourceSystemQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QuerySyncEvents chains the current query on the "sync_events" edge.
func (_q *SourceSystemQuery) QuerySyncEvents() *SyncEventQuery {
	query := (&SyncEventClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(sourcesystem.Table, sourcesystem.FieldID, selector),
			sqlgraph.To(syncevent.Table, syncevent.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, sourcesystem.SyncEventsTable, sourcesystem.SyncEventsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first SourceSystem entity from the query.
// Returns a *NotFoundError when no SourceSystem was found.
func (_q *SourceSystemQuery) First(ctx context.Context) (*SourceSystem, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{sourcesystem.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *SourceSystemQuery) FirstX(ctx context.Context) *SourceSystem {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first SourceSystem ID from the query.
// Returns a *NotFoundError when no SourceSystem ID was found.
func (_q *SourceSystemQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{sourcesystem.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *SourceSystemQuery) FirstIDX(ctx context.Context) string {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single SourceSystem entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one SourceSystem entity is found.
// Returns a *NotFoundError when no SourceSystem entities are found.
func (_q *SourceSystemQuery) Only(ctx context.Context) (*SourceSystem, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{sourcesystem.Label}
	default:
		return nil, &NotSingularError{sourcesystem.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *SourceSystemQuery) OnlyX(ctx context.Context) *SourceSystem {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only SourceSystem ID in the query.
// Returns a *NotSingularError when more than one SourceSystem ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *SourceSystemQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{sourcesystem.Label}
	default:
		err = &NotSingularError{sourcesystem.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *SourceSystemQuery) OnlyIDX(ctx context.Context) string {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of SourceSystems.
func (_q *SourceSystemQuery) All(ctx context.Context) ([]*SourceSystem, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*SourceSystem, *SourceSystemQuery]()
	return withInterceptors[[]*SourceSystem](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *SourceSystemQuery) AllX(ctx context.Context) []*SourceSystem {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of SourceSystem IDs.
func (_q *SourceSystemQuery) IDs(ctx context.Context) (ids []string, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(sourcesystem.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *SourceSystemQuery) IDsX(ctx context.Context) []string {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *SourceSystemQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*SourceSystemQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *SourceSystemQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *SourceSystemQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *SourceSystemQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the SourceSystemQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *SourceSystemQuery) Clone() *SourceSystemQuery {
	if _q == nil {
		return nil
	}
	return &SourceSystemQuery{
		config:         _q.config,
		ctx:            _q.ctx.Clone(),
		order:          append([]sourcesystem.OrderOption{}, _q.order...),
		inters:         append([]Interceptor{}, _q.inters...),
		predicates:     append([]predicate.SourceSystem{}, _q.predicates...),
		withSyncEvents: _q.withSyncEvents.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithSyncEvents tells the query-builder to eager-load the nodes that are connected to
// the "sync_events" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *SourceSystemQuery) WithSyncEvents(opts ...func(*SyncEventQuery)) *SourceSystemQuery {
	query := (&SyncEventClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withSyncEvents = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.SourceSystem.Query().
//		GroupBy(sourcesystem.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *SourceSystemQuery) GroupBy(field string, fields ...string) *SourceSystemGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &SourceSystemGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = sourcesystem.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.SourceSystem.Query().
//		Select(sourcesystem.FieldCreatedAt).
//		Scan(ctx, &v)
func (_q *SourceSystemQuery) Select(fields ...string) *SourceSystemSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &SourceSystemSelect{SourceSystemQuery: _q}
	sbuild.label = sourcesystem.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a SourceSystemSelect configured with the given aggregations.
func (_q *SourceSystemQuery) Aggregate(fns ...AggregateFunc) *SourceSystemSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *SourceSystemQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !sourcesystem.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *SourceSystemQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*SourceSystem, error) {
	var (
		nodes       = []*SourceSystem{}
		_spec       = _q.querySpec()
		loadedTypes = [1]bool{
			_q.withSyncEvents != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*SourceSystem).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &SourceSystem{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withSyncEvents; query != nil {
		if err := _q.loadSyncEvents(ctx, query, nodes,
			func(n *SourceSystem) { n.Edges.SyncEvents = []*SyncEvent{} },
			func(n *SourceSystem, e *SyncEvent) { n.Edges.SyncEvents = append(n.Edges.SyncEvents, e) }); err != nil {
			return nil, err
		}
	}
	for name, query := range _q.withNamedSyncEvents {
		if err := _q.loadSyncEvents(ctx, query, nodes,
			func(n *SourceSystem) { n.appendNamedSyncEvents(name) },
			func(n *SourceSystem, e *SyncEvent) { n.appendNamedSyncEvents(name, e) }); err != nil {
			return nil, err
		}
	}
	for i := range _q.loadTotal {
		if err := _q.loadTotal[i](ctx, nodes); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *SourceSystemQuery) loadSyncEvents(ctx context.Context, query *SyncEventQuery, nodes []*SourceSystem, init func(*SourceSystem), assign func(*SourceSystem, *SyncEvent)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[string]*SourceSystem)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(syncevent.FieldSourceSystemID)
	}
	query.Where(predicate.SyncEvent(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(sourcesystem.SyncEventsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.SourceSystemID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "source_system_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (_q *SourceSystemQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *SourceSystemQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(sourcesystem.Table, sourcesystem.Columns, sqlgraph.NewFieldSpec(sourcesystem.FieldID, field.TypeString))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, sourcesystem.FieldID)
		for i := range fields {
			if fields[i] != sourcesystem.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *SourceSystemQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(sourcesystem.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = sourcesystem.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// WithNamedSyncEvents tells the query-builder to eager-load the nodes that are connected to the "sync_events"
// edge with the given name. The optional arguments are used to configure the query builder of the edge.
func (_q *SourceSystemQuery) WithNamedSyncEvents(name string, opts ...func(*SyncEventQuery)) *SourceSystemQuery {
	query := (&SyncEventClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	if _q.withNamedSyncEvents == nil {
		_q.withNamedSyncEvents = make(map[string]*SyncEventQuery)
	}
	_q.withNamedSyncEvents[name] = query
	return _q
}

// SourceSystemGroupBy is the group-by builder for SourceSystem entities.
type SourceSystemGroupBy struct {
	selector
	build *SourceSystemQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *SourceSystemGroupBy) Aggregate(fns ...AggregateFunc) *SourceSystemGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *SourceSystemGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SourceSystemQuery, *SourceSystemGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *SourceSystemGroupBy) sqlScan(ctx context.Context, root *SourceSystemQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// SourceSystemSelect is the builder for selecting fields of SourceSystem entities.
type SourceSystemSelect struct {
	*SourceSystemQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *SourceSystemSelect) Aggregate(fns ...AggregateFunc) *SourceSystemSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *SourceSystemSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SourceSystemQuery, *SourceSystemSelect](ctx, _s.SourceSystemQuery, _s, _s.inters, v)
}

func (_s *SourceSystemSelect) sqlScan(ctx context.Context, root *SourceSystemQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
