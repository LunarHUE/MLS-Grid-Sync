// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/LunarHUE/MLS-Grid-Sync/ent/attachment"
	"github.com/LunarHUE/MLS-Grid-Sync/ent/attachmentjob"
	"github.com/LunarHUE/MLS-Grid-Sync/ent/media"
	"github.com/LunarHUE/MLS-Grid-Sync/ent/predicate"
	"github.com/LunarHUE/MLS-Grid-Sync/ent/rawoutput"
	"github.com/LunarHUE/MLS-Grid-Sync/ent/syncevent"
	"github.com/google/uuid"
)

// AttachmentJobQuery is the builder for querying AttachmentJob entities.
type AttachmentJobQuery struct {
	config
	ctx            *QueryContext
	order          []attachmentjob.OrderOption
	inters         []Interceptor
	predicates     []predicate.AttachmentJob
	withMedia      *MediaQuery
	withSyncEvent  *SyncEventQuery
	withRawOutput  *RawOutputQuery
	withAttachment *AttachmentQuery
	modifiers      []func(*sql.Selector)
	loadTotal      []func(context.Context, []*AttachmentJob) error
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the AttachmentJobQuery builder.
func (_q *AttachmentJobQuery) Where(ps ...predicate.AttachmentJob) *AttachmentJobQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *AttachmentJobQuery) Limit(limit int) *AttachmentJobQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *AttachmentJobQuery) Offset(offset int) *AttachmentJobQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *AttachmentJobQuery) Unique(unique bool) *AttachmentJobQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *AttachmentJobQuery) Order(o ...attachmentjob.OrderOption) *AttachmentJobQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryMedia chains the current query on the "media" edge.
func (_q *AttachmentJobQuery) QueryMedia() *MediaQuery {
	query := (&MediaClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(attachmentjob.Table, attachmentjob.FieldID, selector),
			sqlgraph.To(media.Table, media.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, attachmentjob.MediaTable, attachmentjob.MediaColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QuerySyncEvent chains the current query on the "sync_event" edge.
func (_q *AttachmentJobQuery) QuerySyncEvent() *SyncEventQuery {
	query := (&SyncEventClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(attachmentjob.Table, attachmentjob.FieldID, selector),
			sqlgraph.To(syncevent.Table, syncevent.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, attachmentjob.SyncEventTable, attachmentjob.SyncEventColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryRawOutput chains the current query on the "raw_output" edge.
func (_q *AttachmentJobQuery) QueryRawOutput() *RawOutputQuery {
	query := (&RawOutputClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(attachmentjob.Table, attachmentjob.FieldID, selector),
			sqlgraph.To(rawoutput.Table, rawoutput.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, attachmentjob.RawOutputTable, attachmentjob.RawOutputColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryAttachment chains the current query on the "attachment" edge.
func (_q *AttachmentJobQuery) QueryAttachment() *AttachmentQuery {
	query := (&AttachmentClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(attachmentjob.Table, attachmentjob.FieldID, selector),
			sqlgraph.To(attachment.Table, attachment.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, attachmentjob.AttachmentTable, attachmentjob.AttachmentColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first AttachmentJob entity from the query.
// Returns a *NotFoundError when no AttachmentJob was found.
func (_q *AttachmentJobQuery) First(ctx context.Context) (*AttachmentJob, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{attachmentjob.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *AttachmentJobQuery) FirstX(ctx context.Context) *AttachmentJob {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first AttachmentJob ID from the query.
// Returns a *NotFoundError when no AttachmentJob ID was found.
func (_q *AttachmentJobQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{attachmentjob.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *AttachmentJobQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single AttachmentJob entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one AttachmentJob entity is found.
// Returns a *NotFoundError when no AttachmentJob entities are found.
func (_q *AttachmentJobQuery) Only(ctx context.Context) (*AttachmentJob, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{attachmentjob.Label}
	default:
		return nil, &NotSingularError{attachmentjob.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *AttachmentJobQuery) OnlyX(ctx context.Context) *AttachmentJob {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only AttachmentJob ID in the query.
// Returns a *NotSingularError when more than one AttachmentJob ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *AttachmentJobQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{attachmentjob.Label}
	default:
		err = &NotSingularError{attachmentjob.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *AttachmentJobQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of AttachmentJobs.
func (_q *AttachmentJobQuery) All(ctx context.Context) ([]*AttachmentJob, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*AttachmentJob, *AttachmentJobQuery]()
	return withInterceptors[[]*AttachmentJob](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *AttachmentJobQuery) AllX(ctx context.Context) []*AttachmentJob {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of AttachmentJob IDs.
func (_q *AttachmentJobQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(attachmentjob.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *AttachmentJobQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *AttachmentJobQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*AttachmentJobQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *AttachmentJobQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *AttachmentJobQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *AttachmentJobQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the AttachmentJobQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *AttachmentJobQuery) Clone() *AttachmentJobQuery {
	if _q == nil {
		return nil
	}
	return &AttachmentJobQuery{
		config:         _q.config,
		ctx:            _q.ctx.Clone(),
		order:          append([]attachmentjob.OrderOption{}, _q.order...),
		inters:         append([]Interceptor{}, _q.inters...),
		predicates:     append([]predicate.AttachmentJob{}, _q.predicates...),
		withMedia:      _q.withMedia.Clone(),
		withSyncEvent:  _q.withSyncEvent.Clone(),
		withRawOutput:  _q.withRawOutput.Clone(),
		withAttachment: _q.withAttachment.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithMedia tells the query-builder to eager-load the nodes that are connected to
// the "media" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *AttachmentJobQuery) WithMedia(opts ...func(*MediaQuery)) *AttachmentJobQuery {
	query := (&MediaClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withMedia = query
	return _q
}

// WithSyncEvent tells the query-builder to eager-load the nodes that are connected to
// the "sync_event" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *AttachmentJobQuery) WithSyncEvent(opts ...func(*SyncEventQuery)) *AttachmentJobQuery {
	query := (&SyncEventClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withSyncEvent = query
	return _q
}

// WithRawOutput tells the query-builder to eager-load the nodes that are connected to
// the "raw_output" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *AttachmentJobQuery) WithRawOutput(opts ...func(*RawOutputQuery)) *AttachmentJobQuery {
	query := (&RawOutputClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withRawOutput = query
	return _q
}

// WithAttachment tells the query-builder to eager-load the nodes that are connected to
// the "attachment" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *AttachmentJobQuery) WithAttachment(opts ...func(*AttachmentQuery)) *AttachmentJobQuery {
	query := (&AttachmentClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withAttachment = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.AttachmentJob.Query().
//		GroupBy(attachmentjob.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *AttachmentJobQuery) GroupBy(field string, fields ...string) *AttachmentJobGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &AttachmentJobGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = attachmentjob.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.AttachmentJob.Query().
//		Select(attachmentjob.FieldCreatedAt).
//		Scan(ctx, &v)
func (_q *AttachmentJobQuery) Select(fields ...string) *AttachmentJobSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &AttachmentJobSelect{AttachmentJobQuery: _q}
	sbuild.label = attachmentjob.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a AttachmentJobSelect configured with the given aggregations.
func (_q *AttachmentJobQuery) Aggregate(fns ...AggregateFunc) *AttachmentJobSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *AttachmentJobQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !attachmentjob.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *AttachmentJobQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*AttachmentJob, error) {
	var (
		nodes       = []*AttachmentJob{}
		_spec       = _q.querySpec()
		loadedTypes = [4]bool{
			_q.withMedia != nil,
			_q.withSyncEvent != nil,
			_q.withRawOutput != nil,
			_q.withAttachment != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*AttachmentJob).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &AttachmentJob{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withMedia; query != nil {
		if err := _q.loadMedia(ctx, query, nodes, nil,
			func(n *AttachmentJob, e *Media) { n.Edges.Media = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withSyncEvent; query != nil {
		if err := _q.loadSyncEvent(ctx, query, nodes, nil,
			func(n *AttachmentJob, e *SyncEvent) { n.Edges.SyncEvent = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withRawOutput; query != nil {
		if err := _q.loadRawOutput(ctx, query, nodes, nil,
			func(n *AttachmentJob, e *RawOutput) { n.Edges.RawOutput = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withAttachment; query != nil {
		if err := _q.loadAttachment(ctx, query, nodes, nil,
			func(n *AttachmentJob, e *Attachment) { n.Edges.Attachment = e }); err != nil {
			return nil, err
		}
	}
	for i := range _q.loadTotal {
		if err := _q.loadTotal[i](ctx, nodes); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *AttachmentJobQuery) loadMedia(ctx context.Context, query *MediaQuery, nodes []*AttachmentJob, init func(*AttachmentJob), assign func(*AttachmentJob, *Media)) error {
	ids := make([]string, 0, len(nodes))
	nodeids := make(map[string][]*AttachmentJob)
	for i := range nodes {
		fk := nodes[i].MediaKey
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(media.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "media_key" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *AttachmentJobQuery) loadSyncEvent(ctx context.Context, query *SyncEventQuery, nodes []*AttachmentJob, init func(*AttachmentJob), assign func(*AttachmentJob, *SyncEvent)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*AttachmentJob)
	for i := range nodes {
		fk := nodes[i].SyncEventID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(syncevent.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "sync_event_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *AttachmentJobQuery) loadRawOutput(ctx context.Context, query *RawOutputQuery, nodes []*AttachmentJob, init func(*AttachmentJob), assign func(*AttachmentJob, *RawOutput)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*AttachmentJob)
	for i := range nodes {
		if nodes[i].RawOutputID == nil {
			continue
		}
		fk := *nodes[i].RawOutputID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(rawoutput.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "raw_output_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *AttachmentJobQuery) loadAttachment(ctx context.Context, query *AttachmentQuery, nodes []*AttachmentJob, init func(*AttachmentJob), assign func(*AttachmentJob, *Attachment)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*AttachmentJob)
	for i := range nodes {
		if nodes[i].AttachmentID == nil {
			continue
		}
		fk := *nodes[i].AttachmentID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(attachment.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "attachment_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (_q *AttachmentJobQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *AttachmentJobQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(attachmentjob.Table, attachmentjob.Columns, sqlgraph.NewFieldSpec(attachmentjob.FieldID, field.TypeUUID))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, attachmentjob.FieldID)
		for i := range fields {
			if fields[i] != attachmentjob.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if _q.withMedia != nil {
			_spec.Node.AddColumnOnce(attachmentjob.FieldMediaKey)
		}
		if _q.withSyncEvent != nil {
			_spec.Node.AddColumnOnce(attachmentjob.FieldSyncEventID)
		}
		if _q.withRawOutput != nil {
			_spec.Node.AddColumnOnce(attachmentjob.FieldRawOutputID)
		}
		if _q.withAttachment != nil {
			_spec.Node.AddColumnOnce(attachmentjob.FieldAttachmentID)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *AttachmentJobQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(attachmentjob.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = attachmentjob.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// AttachmentJobGroupBy is the group-by builder for AttachmentJob entities.
type AttachmentJobGroupBy struct {
	selector
	build *AttachmentJobQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *AttachmentJobGroupBy) Aggregate(fns ...AggregateFunc) *AttachmentJobGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *AttachmentJobGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*AttachmentJobQuery, *AttachmentJobGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *AttachmentJobGroupBy) sqlScan(ctx context.Context, root *AttachmentJobQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// AttachmentJobSelect is the builder for selecting fields of AttachmentJob entities.
type AttachmentJobSelect struct {
	*AttachmentJobQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *AttachmentJobSelect) Aggregate(fns ...AggregateFunc) *AttachmentJobSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *AttachmentJobSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*AttachmentJobQuery, *AttachmentJobSelect](ctx, _s.AttachmentJobQuery, _s, _s.inters, v)
}

func (_s *AttachmentJobSelect) sqlScan(ctx context.Context, root *AttachmentJobQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
