// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"github.com/lunarhue/website-highpointe/packages/mls-grid-sync/ent/attachmentjob"
	"github.com/lunarhue/website-highpointe/packages/mls-grid-sync/ent/predicate"
	"github.com/lunarhue/website-highpointe/packages/mls-grid-sync/ent/rawoutput"
	"github.com/lunarhue/website-highpointe/packages/mls-grid-sync/ent/sourcesystem"
	"github.com/lunarhue/website-highpointe/packages/mls-grid-sync/ent/syncevent"
)

// SyncEventQuery is the builder for querying SyncEvent entities.
type SyncEventQuery struct {
	config
	ctx                     *QueryContext
	order                   []syncevent.OrderOption
	inters                  []Interceptor
	predicates              []predicate.SyncEvent
	withSourceSystem        *SourceSystemQuery
	withRawOutputs          *RawOutputQuery
	withAttachmentJobs      *AttachmentJobQuery
	withOriginalSync        *SyncEventQuery
	withReprocesses         *SyncEventQuery
	modifiers               []func(*sql.Selector)
	loadTotal               []func(context.Context, []*SyncEvent) error
	withNamedRawOutputs     map[string]*RawOutputQuery
	withNamedAttachmentJobs map[string]*AttachmentJobQuery
	withNamedReprocesses    map[string]*SyncEventQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the SyncEventQuery builder.
func (_q *SyncEventQuery) Where(ps ...predicate.SyncEvent) *SyncEventQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *SyncEventQuery) Limit(limit int) *SyncEventQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *SyncEventQuery) Offset(offset int) *SyncEventQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *SyncEventQuery) Unique(unique bool) *SyncEventQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *SyncEventQuery) Order(o ...syncevent.OrderOption) *SyncEventQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QuerySourceSystem chains the current query on the "source_system" edge.
func (_q *SyncEventQuery) QuerySourceSystem() *SourceSystemQuery {
	query := (&SourceSystemClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(syncevent.Table, syncevent.FieldID, selector),
			sqlgraph.To(sourcesystem.Table, sourcesystem.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, syncevent.SourceSystemTable, syncevent.SourceSystemColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryRawOutputs chains the current query on the "raw_outputs" edge.
func (_q *SyncEventQuery) QueryRawOutputs() *RawOutputQuery {
	query := (&RawOutputClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(syncevent.Table, syncevent.FieldID, selector),
			sqlgraph.To(rawoutput.Table, rawoutput.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, syncevent.RawOutputsTable, syncevent.RawOutputsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryAttachmentJobs chains the current query on the "attachment_jobs" edge.
func (_q *SyncEventQuery) QueryAttachmentJobs() *AttachmentJobQuery {
	query := (&AttachmentJobClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(syncevent.Table, syncevent.FieldID, selector),
			sqlgraph.To(attachmentjob.Table, attachmentjob.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, syncevent.AttachmentJobsTable, syncevent.AttachmentJobsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryOriginalSync chains the current query on the "original_sync" edge.
func (_q *SyncEventQuery) QueryOriginalSync() *SyncEventQuery {
	query := (&SyncEventClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(syncevent.Table, syncevent.FieldID, selector),
			sqlgraph.To(syncevent.Table, syncevent.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, syncevent.OriginalSyncTable, syncevent.OriginalSyncColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryReprocesses chains the current query on the "reprocesses" edge.
func (_q *SyncEventQuery) QueryReprocesses() *SyncEventQuery {
	query := (&SyncEventClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(syncevent.Table, syncevent.FieldID, selector),
			sqlgraph.To(syncevent.Table, syncevent.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, syncevent.ReprocessesTable, syncevent.ReprocessesColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first SyncEvent entity from the query.
// Returns a *NotFoundError when no SyncEvent was found.
func (_q *SyncEventQuery) First(ctx context.Context) (*SyncEvent, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{syncevent.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *SyncEventQuery) FirstX(ctx context.Context) *SyncEvent {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first SyncEvent ID from the query.
// Returns a *NotFoundError when no SyncEvent ID was found.
func (_q *SyncEventQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{syncevent.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *SyncEventQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single SyncEvent entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one SyncEvent entity is found.
// Returns a *NotFoundError when no SyncEvent entities are found.
func (_q *SyncEventQuery) Only(ctx context.Context) (*SyncEvent, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{syncevent.Label}
	default:
		return nil, &NotSingularError{syncevent.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *SyncEventQuery) OnlyX(ctx context.Context) *SyncEvent {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only SyncEvent ID in the query.
// Returns a *NotSingularError when more than one SyncEvent ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *SyncEventQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{syncevent.Label}
	default:
		err = &NotSingularError{syncevent.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *SyncEventQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of SyncEvents.
func (_q *SyncEventQuery) All(ctx context.Context) ([]*SyncEvent, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*SyncEvent, *SyncEventQuery]()
	return withInterceptors[[]*SyncEvent](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *SyncEventQuery) AllX(ctx context.Context) []*SyncEvent {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of SyncEvent IDs.
func (_q *SyncEventQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(syncevent.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *SyncEventQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *SyncEventQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*SyncEventQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *SyncEventQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *SyncEventQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *SyncEventQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the SyncEventQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *SyncEventQuery) Clone() *SyncEventQuery {
	if _q == nil {
		return nil
	}
	return &SyncEventQuery{
		config:             _q.config,
		ctx:                _q.ctx.Clone(),
		order:              append([]syncevent.OrderOption{}, _q.order...),
		inters:             append([]Interceptor{}, _q.inters...),
		predicates:         append([]predicate.SyncEvent{}, _q.predicates...),
		withSourceSystem:   _q.withSourceSystem.Clone(),
		withRawOutputs:     _q.withRawOutputs.Clone(),
		withAttachmentJobs: _q.withAttachmentJobs.Clone(),
		withOriginalSync:   _q.withOriginalSync.Clone(),
		withReprocesses:    _q.withReprocesses.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithSourceSystem tells the query-builder to eager-load the nodes that are connected to
// the "source_system" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *SyncEventQuery) WithSourceSystem(opts ...func(*SourceSystemQuery)) *SyncEventQuery {
	query := (&SourceSystemClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withSourceSystem = query
	return _q
}

// WithRawOutputs tells the query-builder to eager-load the nodes that are connected to
// the "raw_outputs" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *SyncEventQuery) WithRawOutputs(opts ...func(*RawOutputQuery)) *SyncEventQuery {
	query := (&RawOutputClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withRawOutputs = query
	return _q
}

// WithAttachmentJobs tells the query-builder to eager-load the nodes that are connected to
// the "attachment_jobs" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *SyncEventQuery) WithAttachmentJobs(opts ...func(*AttachmentJobQuery)) *SyncEventQuery {
	query := (&AttachmentJobClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withAttachmentJobs = query
	return _q
}

// WithOriginalSync tells the query-builder to eager-load the nodes that are connected to
// the "original_sync" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *SyncEventQuery) WithOriginalSync(opts ...func(*SyncEventQuery)) *SyncEventQuery {
	query := (&SyncEventClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withOriginalSync = query
	return _q
}

// WithReprocesses tells the query-builder to eager-load the nodes that are connected to
// the "reprocesses" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *SyncEventQuery) WithReprocesses(opts ...func(*SyncEventQuery)) *SyncEventQuery {
	query := (&SyncEventClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withReprocesses = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.SyncEvent.Query().
//		GroupBy(syncevent.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *SyncEventQuery) GroupBy(field string, fields ...string) *SyncEventGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &SyncEventGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = syncevent.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.SyncEvent.Query().
//		Select(syncevent.FieldCreatedAt).
//		Scan(ctx, &v)
func (_q *SyncEventQuery) Select(fields ...string) *SyncEventSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &SyncEventSelect{SyncEventQuery: _q}
	sbuild.label = syncevent.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a SyncEventSelect configured with the given aggregations.
func (_q *SyncEventQuery) Aggregate(fns ...AggregateFunc) *SyncEventSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *SyncEventQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !syncevent.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *SyncEventQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*SyncEvent, error) {
	var (
		nodes       = []*SyncEvent{}
		_spec       = _q.querySpec()
		loadedTypes = [5]bool{
			_q.withSourceSystem != nil,
			_q.withRawOutputs != nil,
			_q.withAttachmentJobs != nil,
			_q.withOriginalSync != nil,
			_q.withReprocesses != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*SyncEvent).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &SyncEvent{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withSourceSystem; query != nil {
		if err := _q.loadSourceSystem(ctx, query, nodes, nil,
			func(n *SyncEvent, e *SourceSystem) { n.Edges.SourceSystem = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withRawOutputs; query != nil {
		if err := _q.loadRawOutputs(ctx, query, nodes,
			func(n *SyncEvent) { n.Edges.RawOutputs = []*RawOutput{} },
			func(n *SyncEvent, e *RawOutput) { n.Edges.RawOutputs = append(n.Edges.RawOutputs, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withAttachmentJobs; query != nil {
		if err := _q.loadAttachmentJobs(ctx, query, nodes,
			func(n *SyncEvent) { n.Edges.AttachmentJobs = []*AttachmentJob{} },
			func(n *SyncEvent, e *AttachmentJob) { n.Edges.AttachmentJobs = append(n.Edges.AttachmentJobs, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withOriginalSync; query != nil {
		if err := _q.loadOriginalSync(ctx, query, nodes, nil,
			func(n *SyncEvent, e *SyncEvent) { n.Edges.OriginalSync = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withReprocesses; query != nil {
		if err := _q.loadReprocesses(ctx, query, nodes,
			func(n *SyncEvent) { n.Edges.Reprocesses = []*SyncEvent{} },
			func(n *SyncEvent, e *SyncEvent) { n.Edges.Reprocesses = append(n.Edges.Reprocesses, e) }); err != nil {
			return nil, err
		}
	}
	for name, query := range _q.withNamedRawOutputs {
		if err := _q.loadRawOutputs(ctx, query, nodes,
			func(n *SyncEvent) { n.appendNamedRawOutputs(name) },
			func(n *SyncEvent, e *RawOutput) { n.appendNamedRawOutputs(name, e) }); err != nil {
			return nil, err
		}
	}
	for name, query := range _q.withNamedAttachmentJobs {
		if err := _q.loadAttachmentJobs(ctx, query, nodes,
			func(n *SyncEvent) { n.appendNamedAttachmentJobs(name) },
			func(n *SyncEvent, e *AttachmentJob) { n.appendNamedAttachmentJobs(name, e) }); err != nil {
			return nil, err
		}
	}
	for name, query := range _q.withNamedReprocesses {
		if err := _q.loadReprocesses(ctx, query, nodes,
			func(n *SyncEvent) { n.appendNamedReprocesses(name) },
			func(n *SyncEvent, e *SyncEvent) { n.appendNamedReprocesses(name, e) }); err != nil {
			return nil, err
		}
	}
	for i := range _q.loadTotal {
		if err := _q.loadTotal[i](ctx, nodes); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *SyncEventQuery) loadSourceSystem(ctx context.Context, query *SourceSystemQuery, nodes []*SyncEvent, init func(*SyncEvent), assign func(*SyncEvent, *SourceSystem)) error {
	ids := make([]string, 0, len(nodes))
	nodeids := make(map[string][]*SyncEvent)
	for i := range nodes {
		fk := nodes[i].SourceSystemID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(sourcesystem.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "source_system_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *SyncEventQuery) loadRawOutputs(ctx context.Context, query *RawOutputQuery, nodes []*SyncEvent, init func(*SyncEvent), assign func(*SyncEvent, *RawOutput)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*SyncEvent)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(rawoutput.FieldSyncEventID)
	}
	query.Where(predicate.RawOutput(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(syncevent.RawOutputsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.SyncEventID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "sync_event_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *SyncEventQuery) loadAttachmentJobs(ctx context.Context, query *AttachmentJobQuery, nodes []*SyncEvent, init func(*SyncEvent), assign func(*SyncEvent, *AttachmentJob)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*SyncEvent)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(attachmentjob.FieldSyncEventID)
	}
	query.Where(predicate.AttachmentJob(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(syncevent.AttachmentJobsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.SyncEventID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "sync_event_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *SyncEventQuery) loadOriginalSync(ctx context.Context, query *SyncEventQuery, nodes []*SyncEvent, init func(*SyncEvent), assign func(*SyncEvent, *SyncEvent)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*SyncEvent)
	for i := range nodes {
		if nodes[i].ReprocessOf == nil {
			continue
		}
		fk := *nodes[i].ReprocessOf
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(syncevent.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "reprocess_of" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *SyncEventQuery) loadReprocesses(ctx context.Context, query *SyncEventQuery, nodes []*SyncEvent, init func(*SyncEvent), assign func(*SyncEvent, *SyncEvent)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*SyncEvent)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(syncevent.FieldReprocessOf)
	}
	query.Where(predicate.SyncEvent(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(syncevent.ReprocessesColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.ReprocessOf
		if fk == nil {
			return fmt.Errorf(`foreign-key "reprocess_of" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "reprocess_of" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (_q *SyncEventQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *SyncEventQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(syncevent.Table, syncevent.Columns, sqlgraph.NewFieldSpec(syncevent.FieldID, field.TypeUUID))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, syncevent.FieldID)
		for i := range fields {
			if fields[i] != syncevent.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if _q.withSourceSystem != nil {
			_spec.Node.AddColumnOnce(syncevent.FieldSourceSystemID)
		}
		if _q.withOriginalSync != nil {
			_spec.Node.AddColumnOnce(syncevent.FieldReprocessOf)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *SyncEventQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(syncevent.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = syncevent.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// WithNamedRawOutputs tells the query-builder to eager-load the nodes that are connected to the "raw_outputs"
// edge with the given name. The optional arguments are used to configure the query builder of the edge.
func (_q *SyncEventQuery) WithNamedRawOutputs(name string, opts ...func(*RawOutputQuery)) *SyncEventQuery {
	query := (&RawOutputClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	if _q.withNamedRawOutputs == nil {
		_q.withNamedRawOutputs = make(map[string]*RawOutputQuery)
	}
	_q.withNamedRawOutputs[name] = query
	return _q
}

// WithNamedAttachmentJobs tells the query-builder to eager-load the nodes that are connected to the "attachment_jobs"
// edge with the given name. The optional arguments are used to configure the query builder of the edge.
func (_q *SyncEventQuery) WithNamedAttachmentJobs(name string, opts ...func(*AttachmentJobQuery)) *SyncEventQuery {
	query := (&AttachmentJobClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	if _q.withNamedAttachmentJobs == nil {
		_q.withNamedAttachmentJobs = make(map[string]*AttachmentJobQuery)
	}
	_q.withNamedAttachmentJobs[name] = query
	return _q
}

// WithNamedReprocesses tells the query-builder to eager-load the nodes that are connected to the "reprocesses"
// edge with the given name. The optional arguments are used to configure the query builder of the edge.
func (_q *SyncEventQuery) WithNamedReprocesses(name string, opts ...func(*SyncEventQuery)) *SyncEventQuery {
	query := (&SyncEventClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	if _q.withNamedReprocesses == nil {
		_q.withNamedReprocesses = make(map[string]*SyncEventQuery)
	}
	_q.withNamedReprocesses[name] = query
	return _q
}

// SyncEventGroupBy is the group-by builder for SyncEvent entities.
type SyncEventGroupBy struct {
	selector
	build *SyncEventQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *SyncEventGroupBy) Aggregate(fns ...AggregateFunc) *SyncEventGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *SyncEventGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SyncEventQuery, *SyncEventGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *SyncEventGroupBy) sqlScan(ctx context.Context, root *SyncEventQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// SyncEventSelect is the builder for selecting fields of SyncEvent entities.
type SyncEventSelect struct {
	*SyncEventQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *SyncEventSelect) Aggregate(fns ...AggregateFunc) *SyncEventSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *SyncEventSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SyncEventQuery, *SyncEventSelect](ctx, _s.SyncEventQuery, _s, _s.inters, v)
}

func (_s *SyncEventSelect) sqlScan(ctx context.Context, root *SyncEventQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
